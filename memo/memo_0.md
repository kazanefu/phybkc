# 不足している設計情報概要 (最終更新済)

`design.md` のさらなる更新により、すべての懸念事項が解決されました。

## 解決済みの項目
1. **物理キーの識別方法**: `ScanCode` を使用。
2. **プロファイル設定ファイルのスキーマ**: TOML（全体）/JSON（プロファイル）。`keyboard` フィールドの追加。
3. **スクリプトエンジンの仕様**: 構文、優先順位、例外処理（Try/Fail）が定義された。
4. **IPC / 通信**: デーモンの再起動方式を採用。
5. **OS フック**: `SetWindowsHookEx` (WH_KEYBOARD_LL)。
6. **キーボードレイアウト**: プロファイルごとに `JIS/US` 等を指定可能に。
7. **タスクトレイ UI**: メニュー構成が確定。

## 実装の準備完了
これですべての情報が揃いました。次は実装フェーズに移行します。
